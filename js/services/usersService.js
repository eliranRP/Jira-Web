MainApp.factory('usersService', ['$rootScope', '$firebaseArray', '$timeout'
    , '$firebaseObject', '$firebaseStorage',
    function ($rootScope, $firebaseArray, $timeout, $firebaseObject, $firebaseStorage) {
        var db = firebase.firestore();
        var serviceObject = {

            createList: function () {
            }

        }






        return serviceObject;

    }]);